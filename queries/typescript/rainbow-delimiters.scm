; inherits: javascript

(type_arguments
  "<" @opening
  ">" @closing) @container

(type_parameters
  "<" @opening
  ">" @closing) @container

(lookup_type
  "[" @opening
  "]" @closing) @container

(object_type
  "{" @opening
  "}" @closing) @container
